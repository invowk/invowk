# Build artifacts
bin/
/invowk
/invowk-upx
/invowk-cli

# GOOS=windows cross-compile cache (scripts/check-windows-build.sh)
/.cache/

# SSH keys generated during tests/development, in any directory. Private keys
# must never be committed; the server generates its own key at runtime.
# Deliberate test fixtures must use a different name (e.g. testdata/host_key_fixture).
id_ed25519*

# Go build cache
*.test
*.out

# Sonar local analysis artifacts
.scannerwork/
.sonar/reports/

# PGO temporary profiles (keep committed default.pgo)
cpu.prof
mem.prof

# IDE
.vscode/
*.swp
*.swo
*~
.idea/

# OS
.DS_Store
Thumbs.db

# Claude Code local overrides (personal, not shared)
.claude.local.md

# Local agent/tool project state
.antigravitycli/

# Generated benchmark output
artifacts/benchmarks/

# Generated mutation-testing output
artifacts/mutation/

# Release staging
release-assets/

# VHS test output (generated files, not golden files)
vhs/output/
