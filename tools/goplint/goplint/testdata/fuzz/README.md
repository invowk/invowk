# Goplint fuzz seeds

Each target directory contains committed Go fuzz corpus entries for deterministic CI execution. The scheduled/manual fuzz workflow extends these seeds with randomized mutations; any new reproducer must be minimized and committed here.
